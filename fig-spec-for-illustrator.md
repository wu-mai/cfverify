# CF-Verify 方法架构图 — 给画图者的讲解

> 论文要做一张**方法架构图**（paper 第 3 节 Method）。核心一句话：**CF-Verify 用"移除证据再问一次"来判断 LLM 的答案到底有没有依据**。

---

## 4 个模块（每个干啥）

| 模块 | 干啥 | 输出 |
|------|------|------|
| ① **输入** | 三样东西进来：① 问题 q ② 检索到的证据 E（一堆句子）③ 金标准句子 G（标注的"关键证据"） | — |
| ② **Pass 1（完整证据）** | 把**完整证据 E** 喂给 LLM，让它答 → 得到答案 a₁ | a₁ |
| ③ **Pass 2（移除金标准）** | 把**金标准句子 G 从 E 里删掉**，再喂给同一个 LLM 答 → 得到答案 a₂ | a₂ |
| ④ **判定（三选一）** | 比较 a₁ 和 a₂（见下方分支） | 决策 |

**判定分支（第 ④ 模块）：**
- a₁ ≠ a₂（通常 a₂ 变成"证据不足 / insufficient evidence"）→ **✅ grounded（采纳）**
- a₁ = a₂（答案没变）→ **⚠️ revise（修订 / 弃答）**
- a₂ = "证据不足" → **🔄 re-retrieve（重新检索）**

---

## 大致图长这样（左 → 右 数据流）

```
┌─────────┐   ┌──────────┐
│ 问题 q   │   │ 证据 E    │
└────┬────┘   └────┬─────┘
     │             │
     │       ┌─────┴──────┐
     │       │ 金标准 G    │  ← 红色/高亮框
     │       └─────┬──────┘
     │             │
     ▼             ▼
   ┌─────────────────────┐
   │  Pass 1: LLM(E)      │ ──→  a₁   (完整证据下的答案)
   └─────────────────────┘
             │
        [移除 G]   ← 箭头上标 "ablate（消融）"
             ▼
   ┌─────────────────────┐
   │  Pass 2: LLM(E − G)  │ ──→  a₂   (移除金标准后的答案)
   └─────────────────────┘             ↑ 通常 = "insufficient evidence"
             │
             ▼
   ┌─────────────────────┐
   │   比较 a₁ vs a₂       │
   └─────────────────────┘
             │
   ┌─────────┼─────────┐
   ▼         ▼         ▼
 ✅采纳    ⚠️修订    🔄重新检索
(accept)  (revise)  (re-retrieve)
```

---

## 关键视觉要求

- **强调"两次问同一个 LLM"**：图里要清楚体现 Pass 1 和 Pass 2 是**同一个模型**（同一个"LLM"框出现两次，或用同一个颜色），只是输入证据不同（一个完整、一个缺了金标准）
- **金标准 G 用红色 / 高亮框**：让读者一眼看出"被移除的那部分"
- **判定分支用 3 个不同颜色 / 图标的出口**：
  - 采纳 = 绿色 ✓
  - 修订 = 黄色 ⚠
  - 重新检索 = 蓝色 🔄
- **（可选，放右侧）对照柱状图** — 论文核心卖点，两根柱子：
  - 移除金标准 G → 答案翻转率 **93–96%**
  - 移除等量随机句子 → 翻转率 **0–3%**
  - 说明：翻转是**特异于金标准**的，不是"删任何句子都会乱"

---

## 标注建议

- 每个框写名字：`Pass 1` / `Pass 2` / `判定 (Decision)`
- 在"移除 G"那个箭头上标：`ablate（消融）`
- 最终 3 个出口分别标：`accept` / `revise` / `re-retrieve`
- 在 a₂ 那里标个小注：`通常 = "insufficient evidence"`
- LLM 框就写 `LLM`（黑盒），不用画内部结构

---

## 不需要画的

- ❌ 不要画 LLM 内部结构（就是个黑盒框写 "LLM" 即可）
- ❌ 不要画训练流程（这是零训练方法，没有 loss / 反向传播）
- ❌ 不要画数据集细节（HotpotQA / 2Wiki 这些在论文正文，不进架构图）

---

## 文件名 & 位置（给画图者的交付要求）

- 输出文件名：`fig1_architecture.pdf`（矢量 PDF）
- 放到：`figures/fig1_architecture.pdf`
- 论文里会这样引用（LaTeX 端，画图者不用管）：
  ```latex
  \includegraphics[width=\linewidth]{figures/fig1_architecture.pdf}
  ```
